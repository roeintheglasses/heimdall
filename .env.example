# GitHub Personal Access Token (required for webhook automation)
# Create one at: https://github.com/settings/tokens
# Required scopes: 'repo' and 'admin:repo_hook'
GITHUB_TOKEN=ghp_your_token_here

# Custom webhook URL (optional)
# Defaults to: https://heimdall-roeintheglasses.vercel.app/api/webhook
# WEBHOOK_URL=https://your-custom-domain.com/api/webhook

# Webhook secret for payload verification (recommended for security)
# This secret should match the one used in your webhook endpoint
# If not set, a default secret will be used (not recommended for production)
# Also used for GitHub webhook signature verification
WEBHOOK_SECRET=your-secure-webhook-secret-here

# Go service URL for dashboard (if different from default)
# NEXT_PUBLIC_GO_SERVICE_URL=https://your-go-service.railway.app